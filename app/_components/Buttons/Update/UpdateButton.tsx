import { Button } from "antd";

const UpdateButton = () => {
  return (
    <Button
      htmlType="submit"
      className="rounded-none w-24 scale-75 md:scale-100"
      type="primary"
    >
      Update
    </Button>
  );
};

export default UpdateButton;
